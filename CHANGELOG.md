# HY Launcher 패치 노트

> **HY Project** presents · Making World, Making Memories
> 최신 판이 위에 온다.

---

## 표기 규약

- **아직 0.x다.** MOTP와 같은 판단이다 — `1.0.0`은 「배포가 시작되는 날」이 아니라
  **「더 얹을 것이 없는 날」**의 숫자다.
- **다섯 자리가 함께 움직인다**: `client/src/app/version.ts`의 `LAUNCHER_VERSION`(화면에
  찍히는 유일한 곳) · `package.json` 둘(루트·client) · `src-tauri/Cargo.toml` ·
  `src-tauri/tauri.conf.json`(설치본 파일 이름이 여기서 나온다). 어긋나도 `tsc`·`cargo`·
  `vite`가 아무 말을 안 해서 `npm run check:version`이 대신 본다.
- **Windows 설치본은 언어마다 파일이 하나씩이다.** MSI는 언어가 패키지에 구워져 설치
  마법사 안에서 고를 수 없다. 앱 자체의 언어는 설정을 따르므로 어느 것을 받아도 같다.
- **Linux는 AppImage다.** 자가 업데이트가 AppImage에서만 동작한다 — DEB로 주면 새 판이
  나와도 런처가 스스로 갱신하지 못한다.

---

## 0.2.0 — 2026-09-25

**8개 언어로 쓸 수 있고, 게임의 연령 등급을 따른다.** hyproj.com의 지역별 이용약관·연령
등급 정책에 맞춘 판이다.

- **8개 언어** — 한국어·English·Español·Français·Deutsch·日本語·繁體中文(台灣)·繁體中文(香港).
  설정의 「언어」에서 고른다. 「자동」이면 hyproj.com 계정에 정해 둔 언어를, 없으면 운영체제
  언어를 따른다. 게임을 켤 때 뜨는 인사말도 같은 언어로 나온다.
- **오류 안내도 그 언어로** — 설치·동기화·로그인 실패 같은 오류가 고른 언어로 나온다. 한국어가
  아닌 화면에서는 원래 문구를 작은 글씨로 함께 보여 준다. 문의할 때 그대로 옮겨 주시면 된다.
- **연령 등급** — 게임 카드와 게임 화면에 거주 국가의 연령 등급을 표시한다(한국 GRAC, 일본
  CERO, 대만·홍콩·마카오 遊戲軟體分級, 미국 ESRB, 그 밖의 나라 PEGI). 등급에 맞지 않는 게임은
  「연령 제한」으로 보이고 설치·업데이트할 수 없다. 등급이 아직 정해지지 않은 게임은 성인만
  받을 수 있다. **이미 설치한 게임은 계속 실행할 수 있다.** 계정에 생년월일이 없어 나이를
  확인할 수 없으면 hyproj.com 계정 화면으로 안내한다.
- **약관·개인정보 처리방침 안내** — 다시 확인해야 할 문서가 있거나 변경이 예고되면 화면 위에
  알리고, 누르면 hyproj.com에서 전문과 확인 화면을 연다. 확인하기 전에도 런처는 그대로 쓸 수
  있다. 설정에 「약관·개인정보」 항목이 생겨 내게 적용되는 문서를 바로 열 수 있다.
- **미국 계정 안내 (캘리포니아 Digital Age Assurance Act)** — 이 법은 2027년 1월 1일부터 앱이
  실행될 때 운영체제에 사용자의 연령대를 묻도록 한다. 런처는 아직 이 기능을 지원하지 않아 꺼
  두었다. 지원하기 전에 그날이 오면 **거주 국가가 미국인 계정에서만** 런처를 쓸 수 없게 되고,
  런처가 그 이유를 알려 준다. 웹사이트·웹 데모·계정은 그대로 쓸 수 있고 설치된 게임 파일은
  지우지 않는다. Linux는 이 법의 대상이 아니다.

**함께 알아 둘 것**

- 연령 등급 표시와 약관 안내는 hyproj.com이 새 정책으로 바뀐 뒤부터 나타난다. 그 전에는
  0.1.1과 똑같이 동작한다.
- Windows 설치 마법사는 지금처럼 한국어·English·日本語 세 가지다. 런처 화면의 언어는
  어느 설치본을 받았든 설정을 따른다.

---

## 0.1.1 — 2026-09-24

**런처를 다시 켜면 설치한 게임이 「미설치」로 보이던 문제를 고쳤다.**

- **원인** — 런처가 시작할 때 설치 기록(`state.json`)을 읽어 놓고 **쓰지 않았다.** 설치
  직후에는 멀쩡해 보이다가, 런처를 완전히 끄고 다시 켜면 전부 설치되지 않은 것으로 보였다.
  그 상태에서 무엇이든 바꾸면(다른 게임을 설치하는 등) 빈 기록이 원래 기록을 덮어썼다.
- **게임 파일은 지워지지 않았다.** 설치 폴더마다 남겨 둔 표식을 이번 판부터 시작할 때
  읽는다. 기록을 잃은 게임도 **다시 받지 않고** 설치된 것으로 돌아온다.
- 0.1.0에서 기록을 잃은 게임은 클라우드 세이브 설정이 꺼진 채로 돌아올 수 있다. 게임
  화면에서 다시 켜 주세요.

---

## 0.1.0 — 2026-09-24

**첫 판.** HY Project의 게임 설치·업데이트·계정·클라우드 세이브를 게임에서 런처로 옮긴다.

- **게임 설치와 업데이트** — 보유 목록을 hyproj.com에서 받아 `.hyp` 아카이브로 설치한다.
  받다가 끊기면 이어받고, 설치본 교체는 저널을 쓴 뒤 rename 두 번으로 한다. 그 사이에
  전원이 나가도 다음 실행 때 끝내거나 되돌린다.
- **HY 계정 연결** — 시스템 브라우저에서 로그인한다. 런처는 비밀번호를 보지 않는다.
  refresh token은 OS 키체인에 둔다.
- **클라우드 세이브** — 게임을 켤 때 내려받고 끄면 올린다. 게임마다 켜고 끌 수 있고,
  클라우드·로컬 세이브를 각각 지울 수 있다. 두 기기의 저장본이 갈리면 **합치지 않고**
  한쪽을 고르게 한다 — 고르지 않은 쪽은 7일간 보관한다.
- **버전 고르기** — 게임마다 **최신 릴리스 / 최신(테스트 버전 포함) / 특정 버전 고정** 중
  하나를 고른다. 고정하면 새 판이 나와도 업데이트를 권하지 않는다 — 모드는 특정 게임
  버전에 맞춰 만들어지므로, 모드를 쓰는 사람에게는 「이 버전에 머무르기」가 필요하다.
  이전 버전으로 내려갈 때는 세이브가 호환되지 않을 수 있다고 한 번 묻는다.
- **비공개 테스트 코드** — 받은 코드를 라이브러리 화면이나 게임 화면에 넣으면 그 게임의
  비공개 테스트 버전이 버전 목록에 나타난다. 한 번 넣으면 이후의 테스트 버전도 전부
  보인다. 아직 보유하지 않은 게임의 코드라면 보유도 함께 받는다.
- **실행 인사** — 게임을 띄울 때 화면 위에 잠깐 인사말을 보여 준다. 설정에서 끌 수 있다.
- **딥링크** — `hylauncher://game/<슬러그>`로 웹사이트에서 바로 그 게임 화면을 연다.
- **자가 업데이트** — 실행할 때마다 확인한다. 서명된 매니페스트만 받아들인다.

**제거해도 세이브는 남는다.** 게임을 지울 때 세이브를 따로 보관했다가, 다시 설치하면
돌려준다. 방금 끈 게임이 폴더를 잠깐 쥐고 있어 옮기지 못하면 몇 초 기다렸다 다시 하고,
그래도 안 되면 세이브를 제자리로 되돌린 뒤 무엇을 확인하면 되는지 알려 준다.

**지운 것은 바로 사라지지 않는다.** 제거·세이브 삭제·설치본 교체 모두 `.trash/`를 거치고
7일 뒤에 정리된다. Windows에서 방금 죽은 게임 프로세스가 핸들을 쥐고 있으면 즉시 삭제가
실패하는데, 그 실패가 설치 성공을 뒤집으면 안 되기 때문이기도 하다.
